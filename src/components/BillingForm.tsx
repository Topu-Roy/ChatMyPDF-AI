import { getUserSubscriptionPlan } from '@/lib/stripe'
import { SubscriptionPlanType } from '@/types/subscription-form';
import React from 'react'

function BillingForm({ }: SubscriptionPlanType) {
    return (
        <div>BillingForm</div>
    )
}

export default BillingForm;